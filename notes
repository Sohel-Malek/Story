git init > t initialize git for version control


staging area > a intermediate place were you can pick and choose which files insdeite dir you want to commit 
>git status

>git add file.txt : add to staging area

>git commit -m "Initial commit"
Try to use present tense 

>git log

>git diff file.txt

>git checkout file.txt 

$ git commit -m "Completed Chapter 1"
Author identity unknown

*** Please tell me who you are.

Run

  git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

to set your account's default identity.
Omit --global to set the identity only in this repository.

fatal: unable to auto-detect email address (got 'admin@Friday-2.(none)')

>>>
>git remote add origin https://github.com/Sohel-Malek/Story.git

git push -u origin master

>git branch alien-plot
>git branch
*
>git checkout alien-plot